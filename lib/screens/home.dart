import 'package:flutter/material.dart';

class Machinehom extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Home"),
        backgroundColor: Colors.cyan,
      ),
      body: Column(
        children: [
          Text("hey"),
        ],
      ),
    );
  }

}